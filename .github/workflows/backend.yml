name: backend-ci
on: [push]
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - run: echo '[Container] Phase complete: BUILD SUCCESS' > backend.log
   - run: aws s3 cp backend.log s3://mp4-logs-635114519658/backend-logs/backend-log.gz
     env:
       AWS_DEFAULT_REGION: us-east-1
